/*
   --- @author leosncz
   --- SmallModularOperatingSystem (SMOS) kernel header file 
   --- Only purpose is to define vital kernel functions
*/

#ifndef KERNEL
#define KERNEL
void start(); // Kernel entry point, responsible of various setup
void kmain(); // Main kernel point where the fun begins
#endif

